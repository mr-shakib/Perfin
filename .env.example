# Supabase Configuration
# Get these from: https://supabase.com → Your Project → Settings → API
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-actual-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-actual-service-role-key-here

# Note: The service role key should NEVER be used in client-side code
# It's included here for backend/admin operations only
# Only use SUPABASE_ANON_KEY in the Flutter app

# Groq AI Configuration (Primary AI Service)
# Get your API key from: https://console.groq.com/keys
GROQ_API_KEY=gsk_your_groq_api_key_here

# Google Gemini AI Configuration (DEPRECATED - Not in use)
GEMINI_API_KEY=your_gemini_api_key_here

# INSTRUCTIONS:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual credentials
# 3. Never commit .env to version control (it's in .gitignore)

# HOW TO GET SUPABASE CREDENTIALS:
# 1. Go to https://supabase.com and sign in
# 2. Create a new project or select existing project
# 3. Go to Project Settings → API
# 4. Copy:
#    - Project URL → SUPABASE_URL
#    - anon/public key → SUPABASE_ANON_KEY (starts with eyJ, ~200 chars)
#    - service_role key → SUPABASE_SERVICE_ROLE_KEY (starts with eyJ, ~200 chars)

# HOW TO GET GROQ API KEY:
# 1. Go to https://console.groq.com
# 2. Sign up or sign in
# 3. Go to API Keys section
# 4. Create a new API key
# 5. Copy the key (starts with gsk_)
